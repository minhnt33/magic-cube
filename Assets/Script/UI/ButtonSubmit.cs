using UnityEngine;
using System.Collections;

public class ButtonSubmit : MonoBehaviour
{
    public void Submit()
    {
        //GPManager.Instance.SubmitScore(PrefManager.Instance.GetCurrentScore());
    }
}
